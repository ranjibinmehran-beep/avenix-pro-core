# 🚀 راهنمای نصب فیکس — Avenix Pro Gold

**فایل دانلود:** `Avenix_Fix.zip` (۲۲ کیلوبایت)

محتویات:
| فایل | توضیح |
|---|---|
| `dashboard.py` | ✅ فایل اصلی فیکس‌شده — همین را جایگزین کن |
| `requirements.txt` | اختیاری — `streamlit` بهش اضافه شده |
| `FIX_README.md` | توضیح فنی باگ‌ها |

---

## ⚠️ چرا دفعه‌ی قبل اعمال نشد

کامیت `74d2c13` که آپلود کردی **هیچ تغییری نداشت** — گیت‌هاب همان فایل قدیمی را دید و کامیت خالی ثبت کرد. برای همین این بار از مسیر **مداد (✏️)** برو، نه «Upload files».

---

## روش ۱ — ویرایش مستقیم (پیشنهادی، روی موبایل هم کار می‌کند)

۱. زیپ را دانلود و باز کن، `dashboard.py` را با یک ویرایشگر متن باز کن
۲. **کل متن** را انتخاب کن → کپی (Ctrl+A سپس Ctrl+C)
۳. برو به:
   `github.com/ranjibinmehran-beep/avenix-pro-core`
۴. روی فایل **`dashboard.py`** بزن
۵. آیکن **مداد ✏️** بالا سمت راست
۶. کل متن قدیمی را **پاک** کن (Ctrl+A سپس Delete)
۷. متن جدید را **Paste** کن
۸. پایین صفحه → دکمه‌ی سبز **Commit changes**

### ✅ چطور بفهمی موفق شدی

بعد از کامیت، در تاریخچه باید ببینی:

```
+43 −0     (۴۳ خط اضافه، صفر حذف)
```

اگر نوشت **`0 changes`** یا **`Showing 0 changed files`** یعنی باز هم اعمال نشده — به من بگو.

---

## روش ۲ — دستور گیت (اگر کامپیوتر داری)

```bash
git clone https://github.com/ranjibinmehran-beep/avenix-pro-core.git
cd avenix-pro-core

# فایل فیکس‌شده را جایگزین کن
cp /مسیر/دانلود/dashboard.py .

git add dashboard.py
git commit -m "Fix NameError: add missing selected_sens and prop_dd_val widgets"
git push
```

---

## ۳. ری‌دیپلوی

Streamlit Cloud معمولاً خودکار ری‌دیپلوی می‌کند. اگر نکرد:

`share.streamlit.io` → اپ `avenix-pro-m` → منوی ⋮ → **Reboot app**

---

## 🔍 چه چیزی عوض شد

فقط **۴۳ خط اضافه** شد، هیچ خطی حذف یا تغییر نکرد:

| خط | تغییر |
|---|---|
| ~۱۰۳۷ | selectbox حساسیت موتور تحلیل (`selected_sens`) |
| ~۱۰۷۳ | دو اسلایدر سقف دراودان (`prop_dd_val`, `prop_overall_dd_val`) |
| ~۱۲۲۲ | ذخیره‌ی `prop_overall_drawdown_limit` که جا افتاده بود |

---

## ✅ تست‌های انجام‌شده روی همین فایل داخل زیپ

| # | تست | نتیجه |
|---|---|---|
| ۱ | بارگذاری اپ | بدون exception ✅ |
| ۲ | رد کردن دیسکلیمر | ✅ |
| ۳ | حضور ویجت‌های جدید | ۱ selectbox + ۲ slider ✅ |
| ۴ | **کلیک دکمه‌ی Save** | بدون NameError ✅ |
| ۵ | ذخیره‌ی واقعی مقادیر | `sensitivity=high`, `prop_dd=3.7` ✅ |

`md5: 44928beafaa7ba8a5686bbf6df8bed3c`

---

## بعد از دیپلوی

اگر خطای جدیدی دیدی اسکرین‌شات بفرست. اگر همه‌چیز درست بالا آمد، قدم بعدی وصل کردن `risk_shield` (سپر ۲۷-تستی که ساختیم) به `execution.py` است.
